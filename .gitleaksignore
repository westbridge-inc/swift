# Fingerprint allowlist — verified NON-secrets only. Format: <commit>:<file>:<rule>:<line>
# boot-config.test.ts asserts the OTP secret VALIDATOR accepts a >=32-char value; the
# fixture string is literally named "dedicated-otp-secret-at-least-32-chars" and guards
# real config hygiene. Verified false positive 2026-08-15 (baseline scan) + 2026-08-17.
a9098c8ff4db34dd95f2d334f674d9c564452b60:apps/api/src/__tests__/boot-config.test.ts:generic-api-key:57
# rate-limit-key.test.ts (draft commit of AVAIL-1) used a hand-written token-SHAPED string
# ({"alg":"HS256"}.{"userId":"forger"}.bad-signature) to prove a wrongly-signed bearer is
# keyed by IP, never by the token. Not a credential: no real signature, no real secret.
# The finishing commit assembles it at runtime instead; this entry covers the draft commit.
# Verified false positive 2026-09-23 (AVAIL-1 finishing lane).
894e1610a391765686429e5d11ae16266c41f00c:apps/api/src/__tests__/rate-limit-key.test.ts:jwt:74
