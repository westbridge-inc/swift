import 'package:flutter/material.dart';

/// Swift design system colors — premium black & white palette.
class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF000000);
  static const secondary = Color(0xFF8E8E93);

  // Backgrounds
  static const background = Color(0xFFF8F8F8);
  static const surface = Color(0xFFFFFFFF);
  static const inputBackground = Color(0xFFF2F2F7);

  // Text
  static const textPrimary = Color(0xFF1C1C1E);
  static const textSecondary = Color(0xFF636366);
  static const textTertiary = Color(0xFFAEAEB2);

  // Borders
  static const border = Color(0xFFE5E5EA);
  static const divider = Color(0xFFF2F2F7);

  // Status
  static const success = Color(0xFF34C759);
  static const error = Color(0xFFFF3B30);
  static const warning = Color(0xFFFF9500);
  static const info = Color(0xFF007AFF);

  // Module Accents
  static const rides = Color(0xFF000000);
  static const eats = Color(0xFF34C759);
  static const courier = Color(0xFF007AFF);

  // Surge
  static const surgeActive = Color(0xFFFF3B30);
  static const surgeBackground = Color(0xFFFFF3F3);
}
