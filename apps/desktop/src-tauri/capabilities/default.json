{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core only \u2014 the app has no native powers beyond the keychain commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}