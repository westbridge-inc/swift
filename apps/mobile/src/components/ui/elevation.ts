/**
 * Depth comes from the token scale — promoted into @swift/ui (design-100×
 * foundation) so kit, legacy kit and web share ONE elevation decision.
 * Kept as a re-export so existing `components/ui` imports keep working.
 */
export { elevation } from '@swift/ui';
