// Guyana — the launch market and the app's existing default (the phone screen
// already defaults to +592). Customers browse straight away on this while their
// real market resolves from device location; it's never left empty.
export const DEFAULT_COUNTRY = {
  code: 'GY',
  dialCode: '+592',
  currencyCode: 'GYD',
  currencySymbol: '$',
} as const;
